PORT=3000
APP_TITLE=Lifecycle Lookup
APP_SUBTITLE=Check end-of-life and release cadence data for common technologies before approving upgrades.
WORKSHOP_HINT=The team usually starts with built-in feeds, but the fetcher also accepts a custom JSON source when vendor data lives elsewhere.
